export default async function handler(req,res){if(req.method!=="GET")return res.status(405).json({error:"허용되지 않은 요청입니다."});const webhook=process.env.GOOGLE_SHEETS_WEBHOOK_URL,{start,end}=req.query;if(!webhook)return res.status(500).json({error:"구글시트 연결 정보가 없습니다."});try{const url=new URL(webhook);url.searchParams.set("action","availability");url.searchParams.set("start",start);url.searchParams.set("end",end);const r=await fetch(url),data=await r.json();if(!r.ok||data.ok===false)throw 0;res.setHeader("Cache-Control","no-store");return res.status(200).json({closedDates:data.closedDates||[],bookedSlots:data.bookedSlots||[]})}catch{return res.status(500).json({error:"예약 일정을 불러오지 못했습니다."})}}
